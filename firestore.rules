rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // O site publicado não utiliza Firestore.
    // Acesso negado por padrão para impedir exposição acidental de dados.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
